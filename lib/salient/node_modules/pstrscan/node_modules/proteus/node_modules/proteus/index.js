/**
 * @module Proteus
 * @class Proteus
 * @static
 */
var putil   = require("proteus/util");

putil.merge(
    module.exports,
    putil,
    require("proteus/properties"),
    require("proteus/create"),
    require("proteus/class"),
    require("proteus/proto"),
    require("proteus/bind")
);
